# comment everything out as I had problems with some gcc compilation with a non-empty Rprofile
# library(testthat)
# if(interactive()) try(fortunes::fortune(), silent = TRUE)
# do no
#print("Old packages: ")
#utils::old.packages()
